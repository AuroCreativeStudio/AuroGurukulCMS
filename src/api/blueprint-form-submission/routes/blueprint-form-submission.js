'use strict';

/**
 * blueprint-form-submission router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::blueprint-form-submission.blueprint-form-submission');
