'use strict';

/**
 * blueprint-form-submission service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::blueprint-form-submission.blueprint-form-submission');
