'use strict';

/**
 * cookie-consent controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::cookie-consent.cookie-consent');
